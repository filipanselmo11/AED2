#include <iostream>

using namespace std;

void stringmatchIngenuo(string text, string padrao){
    int n = text.size();
    int m = padrao.size();
    for(int s = 0; s < n - m; s++){
        for(int i = 0; i < n; i++){
            if(){
                
            }
        }
    }
}